sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Ingestion — data loading and web scraping
pandas>=2.0
openpyxl>=3.1
requests>=2.31
beautifulsoup4>=4.12

# Query interface
gradio>=6.9.0

# Document ingestion — optional; required for PDF sources (source 2):
pdfplumber==0.11.4

# JS-rendered pages — optional; required for Student Sublet (source 7):
playwright>=1.40
# Run after install: playwright install chromium
